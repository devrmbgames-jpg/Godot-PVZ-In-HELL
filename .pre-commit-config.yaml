repos:
  - repo: local
    hooks:
      - id: project-structure
        name: validate project structure
        entry: python utils/validate_project_structure.py
        language: system
        pass_filenames: false
        always_run: true

      - id: staged-diff-check
        name: git diff --cached --check
        entry: git diff --cached --check
        language: system
        pass_filenames: false
        always_run: true
